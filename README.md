# cieloMaquininhas
  
# front end

  - npm install -g @angular/cli
  - npm install 
  - cd front-end/
  - npm install
  - ng serve --open

# back end

  - cd back-end/
  - npm install
  - npm run dev / node server.js
